using UnityEngine;

[RequireComponent(typeof(Collider))]
public class ClickDetector : MonoBehaviour
{
    // Setați acest nume în Inspector (ex: "TriggerHatch")
    public string animationTriggerName = "Hatch"; 

    private void OnMouseDown()
    {
      
    }
}